# Copy to functions/.env (local emulator) or functions/.env.<projectId> (deploy).
# Non-secret params only — secrets go through `firebase functions:secrets:set`.
APP_BASE_URL=https://your-project.web.app
GOOGLE_REDIRECT_URI=https://your-project.web.app/api/google/callback
EMAIL_FROM=Bookings <bookings@yourdomain.com>
TWILIO_FROM_NUMBER=
